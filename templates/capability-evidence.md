# Capability evidence v1

Record exact observed evidence, not broad trusted booleans.

- Schema version: 1
- Worker ID:
- Requested model:
- Resolved model (use `unknown`, never infer):
- Runtime fingerprint:
- Observed runtime fingerprint:
- Evidence status: verified / expired / unknown / unavailable
- Verified at (timestamp with timezone):
- Expires at (timestamp with timezone):
- Effective context tokens:
- Verified task classes (array):
- Verified reasoning levels (array):
- Exact verified tools (array):
- Verified output capabilities (array):
- Permitted data classes (array):
- Verification methods (array):
- Evidence reference:
- Route available: true / false
- Hard spend stop verified: true / false

Every required dimension is matched independently. Changed runtime fingerprints, expired/unknown evidence or unknown resolved identity disqualify automatic routing.
