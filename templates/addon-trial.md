# Optional add-on trial

A private record for a separately approved scope. It is not an installer payload or a completed test result.

- Existing native/API baseline and route passport:
- Why this add-on is needed; what the baseline already does:
- Exact component version/pin and isolated owner/runtime:
- Main model/effort/auth/permissions that remain unchanged:
- Optional worker model change and separate consent, if applicable:
- Official provider/account/project, entitlement and data policy:
- Fixed model/fallback/connection controls and how they are enforced:
- Account-level spend stop and auto-billing state; unknown is not zero:
- Chosen compression owner and eligible source domain:
- Independent transforms/caches/logging/sync that must be disabled or accepted:
- Full original retention and demonstrated retrieval:
- Effective settings and readback evidence, without secrets:
- Explicit approval before provider creation/testing or any upstream traffic:
- Negative cases: bad key, forbidden model/connection, unknown quota/price, 402/429/5xx, overflow, empty result, cancel and concurrent requests:
- Actual upstream attempts/model/account, not just requested settings:
- Paired complete-task baseline and candidate outputs:
- Parent/helper/input/output/cache/retry/restore/verification costs:
- Quality, coverage, latency and human repair:
- Keep, narrow or revert decision and exact rollback verification:

OmniRoute: do not treat header-off, a price-zero label, a zero budget or a modelPinned field as proof of full bypass, free inference or fixed-model behavior. Use the documented gate for the exact route.
